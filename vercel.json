{
  "version": 2,
  "installCommand": "if [ -d blog-frontend ]; then npm install && cd blog-frontend && npm install; else npm install; fi",
  "buildCommand": "if [ -d blog-frontend ]; then cd blog-frontend && npm run build; else npm run build; fi",
  "outputDirectory": ".next"
}
